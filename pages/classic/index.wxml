<view class="header">
  <v-date class="date" index="{{classic.index}}" />
  <v-like class="like" like="{{!!classic.like_status}}" count="{{classic.fav_nums}}" bind:like="onLike" />
</view>
<v-movie movieImgSrc="{{classic.image}}" movieContent="{{classic.content}}" />