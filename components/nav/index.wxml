<view class="container">
  <image class="icon" src="{{first?disImgLeft:imgLeft}}" bind:tap="onLeft"></image>
  <text class="title">{{title}}</text>
  <image class="icon" src="{{latest?disImgRight:imgRight}}" bind:tap="onRight"></image>
</view>