page {
  /* iOs 默认字体 "PingFangSC-Thin" */
  /* android 默认字体 思源 */
  font-family: "Source Han Sans CN";
  font-size: 32rpx;
}

.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.container .img {
  width: 750rpx;
  height: 500rpx;
}

.container .tag {
  width: 46rpx;
  height: 142rpx;
  position: relative;
  bottom: 58rpx;
  right: 310rpx;
}

.container .content {
  font-size: 36rpx;
  max-width: 550rpx;
}